package calebxzhou.rdi.core.constant

/**
 * Created  on 2023-02-24,23:30.
 */
object RdiSounds {
    private const val dir = "sounds/"
    private const val ext = ".ogg"
    const val Connect = "${dir}connect${ext}"
    const val Disconnect = "${dir}disconnect${ext}"
    const val Settings = "${dir}settings${ext}"
    const val Startup = "${dir}startup${ext}"
}
